/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'placehold.co','source.unsplash.com',"images.unsplash.com"],
      }
};

export default nextConfig;
